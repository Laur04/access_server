---

- hosts: "{{ lookup('env', 'STATUS_HOSTNAME') }}"
  remote_user: controller
  tasks:
    - shell: ping "{{ lookup('env', 'ADDRESS') }}"
      async: 5
      poll: 0
      args:
        executable: /bin/bash
      register: ps

    - debug: var=ps.stdout_lines
